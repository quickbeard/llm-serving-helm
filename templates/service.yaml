apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-llm-serving.fullname" . }}
  labels:
    {{- include "helm-llm-serving.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-llm-serving.selectorLabels" . | nindent 4 }}
