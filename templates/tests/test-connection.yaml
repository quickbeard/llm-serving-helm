apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-llm-serving.fullname" . }}-test-connection"
  labels:
    {{- include "helm-llm-serving.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-llm-serving.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
